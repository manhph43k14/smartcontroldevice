<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/antiqueWhile">

    <LinearLayout
        android:id="@+id/llMain"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:orientation="vertical">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbarControl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimaryDark">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginVertical="@dimen/titleMargin"
                android:text="@string/app_name"
                android:textColor="@android:color/white"
                android:textSize="@dimen/appNameTextSize"
                android:textStyle="bold"
                tools:ignore="SpUsage,UnusedAttribute" />
        </android.support.v7.widget.Toolbar>

        <ImageView
            android:id="@+id/imgElectronic"
            android:layout_width="@dimen/imgSize"
            android:layout_height="@dimen/imgSize"
            android:layout_marginTop="@dimen/imgMarginTop"
            android:src="@drawable/ic_btn_electronic"
            tools:ignore="ContentDescription" />

        <ImageView
            android:id="@+id/imgRing"
            android:layout_width="@dimen/imgSize"
            android:layout_height="@dimen/imgSize"
            android:layout_marginTop="@dimen/imgMarginTop"
            android:src="@drawable/ic_btn_ring"
            tools:ignore="ContentDescription" />

        <ImageView
            android:id="@+id/imgDoor"
            android:layout_width="@dimen/imgSize"
            android:layout_height="@dimen/imgSize"
            android:layout_marginTop="@dimen/imgMarginTop"
            android:src="@drawable/ic_btn_door"
            tools:ignore="ContentDescription" />
    </LinearLayout>

    <android.support.v7.widget.RecyclerView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/recyclerViewOption"
        android:layout_width="250dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@android:color/white"
        android:fitsSystemWindows="true" />

</android.support.v4.widget.DrawerLayout>